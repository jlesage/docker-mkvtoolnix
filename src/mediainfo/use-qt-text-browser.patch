--- a/Project/QMake/GUI/MediaInfoQt.pro
+++ b/Project/QMake/GUI/MediaInfoQt.pro
@@ -10,7 +10,7 @@ win32 {
     qtHaveModule(webenginewidgets) {
         QT += webenginewidgets
     } else {
-        DEFINES+=MEDIAINFO_HTML_NO
+        DEFINES+=QT_TEXT_BROWSER_YES
     }
 }
 
--- a/Source/GUI/Qt/htmlwidget.h	2025-07-29 15:00:06.405946470 -0400
+++ b/Source/GUI/Qt/htmlwidget.h	2025-07-29 15:01:05.127698591 -0400
@@ -18,6 +18,9 @@
 #ifdef EDGE_WEBVIEW2_YES
 #include "edgewebview2widget.h"
 #define WebViewWidget WebView2Widget
+#elif defined(QT_TEXT_BROWSER_YES)
+#include <QTextBrowser>
+#define WebViewWidget QTextBrowser
 #else
 #include <QWebEngineView>
 #define WebViewWidget QWebEngineView
--- a/Source/GUI/Qt/htmlwidget.cpp	2025-07-29 15:00:11.036039682 -0400
+++ b/Source/GUI/Qt/htmlwidget.cpp	2025-07-29 15:06:48.535271993 -0400
@@ -91,6 +91,9 @@
     else
         HTML = wstring2QString(C->Inform_Get());
 
+#ifdef QT_TEXT_BROWSER_YES
+    webView->setHtml(HTML);
+#else
     if (HTML.toUtf8().size() < 0.5e6)
         webView->setHtml(HTML);
     else {
@@ -100,6 +103,7 @@
         tempFile.write(HTML.toUtf8());
         webView->load(QUrl::fromLocalFile(tempFile.fileName()));
     }
+#endif
 }
 
 void HTMLViewWidget::changeFilePos(int newFilePos) {
